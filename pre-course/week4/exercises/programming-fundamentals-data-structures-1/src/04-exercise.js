/**
 * Use the built in `push()` method of arrays
 * to add new names to the names array.
 *
 * Add the names:
 * `Ana` and `Maria`
 * using `push()` to the `names` array.
 */
function exercise04() {
    const names = ['John', 'Mark'];

    // Write your solution bellow this line
    names.push("Ana");
    names.push("Maria");
    // Don’t change the code bellow this line
    return names;
}

module.exports = exercise04;
