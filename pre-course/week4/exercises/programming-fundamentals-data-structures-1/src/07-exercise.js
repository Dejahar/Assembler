/**
 * Use the built in `unshift()` method of arrays
 * to add a new name "Alex" to the beginning of the `names` array.
 *
 * Add the name 'Alex' to the names array using `unshift()`
 */
function exercise07() {
    const names = ['Mark', 'Ana'];

    // Write your solution bellow this line
    names.unshift("Alex");
    // Don’t change the code bellow this line
    return names;
}

module.exports = exercise07;
